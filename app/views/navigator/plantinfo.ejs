<div>
    <!--put plant info into here after popup show image, name, description, and show heatmap-->
    <div>
        <h1>EJs plant name</h1>
        <a href="" alt="plant"></a>
        <p>EJs plant description</p>
        <div>
        <h2>Health Heatmap</h2>
            <div>
                <div id="map" style="height: 400px;"></div>
                <script>
                    // Initialize Leaflet map
                    const map = L.map('map').setView([51.505, -0.09], 13);

                    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                        attribution: '© OpenStreetMap contributors',
                        maxZoom: 19
                    }).addTo(map);

                    // Sample heatmap data points [lat, lng, intensity]
                    const heatmapData = [
                        [51.5, -0.09, 0.5],
                        [51.51, -0.1, 0.7],
                        [51.52, -0.08, 0.9]
                    ];

                    // Create and add heatmap layer
                    const heat = L.heatLayer(heatmapData, {
                        radius: 25,
                        blur: 15,
                        maxZoom: 1
                    }).addTo(map);
                </script>
            </div>
        </div>
    </div>
    
</div>